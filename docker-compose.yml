services:
  playtak-bot:
    image: mortenlohne/tiltak-playtak:0.1.0
    container_name: playtak-bot
    restart: unless-stopped
    environment:
      PLAYTAK_USERNAME: "testuser2"
      PLAYTAK_PASSWORD: "pw"
      SIZE: "5"
      TC: "300+10"
      PLAYTAK_BASE_URL: "host.docker.internal"
      PLAYTAK_PORT: "10000"
      KOMI: "0.0"
